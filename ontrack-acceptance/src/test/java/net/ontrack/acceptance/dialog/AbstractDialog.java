package net.ontrack.acceptance.dialog;

import net.ontrack.acceptance.pages.AbstractPage;
import org.openqa.selenium.WebDriver;

public abstract class AbstractDialog extends AbstractPage {

    public AbstractDialog(WebDriver driver) {
        super(driver);
    }
}
