# Git extension

extension.git
    en -> Git

git.remote
    en -> Git repository to clone
    fr -> Repository Git à cloner

git.branch
    en -> Git branch
    fr -> Branche Git

git.tag
    en -> Git tag pattern
    fr -> Motif pour les tags Git

[changelog]

git.changelog
    en -> Change log
    fr -> Changements
